import React from "react";
import { Button } from "@mui/material";

class GenerateButton extends React.Component {
  handleOnClick() {
    return;
  }

  render() {
    return <Button id="generateButton">Generate</Button>;
  }
}

export default GenerateButton;
